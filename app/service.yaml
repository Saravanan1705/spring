apiVersion: v1
kind: Service
metadata:
  labels:
    app: spring-petclinic
  name: spring-petclinic
spec:
  ports:
  - name: 3000-tcp
    port: 8080
    protocol: TCP
    targetPort: 3000
  selector:
    app: spring-petclinic
  sessionAffinity: None
  type: ClusterIP
